name: Go Build Develop

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Install dependencies
      run: |
        sudo apt update --yes && sudo apt upgrade --yes && sudo apt install --yes \
            build-essential \
            locales \
            fakeroot \
            devscripts \
            libxtst-dev \
            libgtk2.0-dev \
            libgtk-3-dev \
            libanthy-dev \
            libchewing3-dev \
            libappindicator-dev \
            libappindicator3-dev \
            qtbase5-private-dev

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.15

    - name: Build
      run: go build -v -ldflags -H=windowsgui -o build/grif.exe ./grif.go
      
    - uses: "marvinpinto/action-automatic-releases@latest"
      with:
        repo_token: "${{ secrets.RELEASE_TOKEN }}"
        automatic_release_tag: "latest"
        prerelease: true
        title: "Development Build"
        files: |
          go.exe
